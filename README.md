# Pastebin Lite

A simple Next.js 13 Pastebin-like app.

## Running locally
1. `npm install`
2. `npm run dev`

## API
- `GET /api/healthz` - health check
- `POST /api/pastes` - create paste
- `GET /api/pastes/:id` - fetch paste
- `GET /p/:id` - view paste
